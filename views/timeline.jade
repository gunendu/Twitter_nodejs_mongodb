h2 Timeline
i Latest registered users (an example of SORT command!)

div
    - if (doc.length) {
        - doc.forEach(function(user) {
            a(class="username", href="/profile?u="+user.userId + ":" +user.username) #{user.username}
            |
        - })
    - }

i Latest 50 messages from users around the world!

- if (posts.length)
    - posts.forEach(function(post) {
      div(class='post')
        a(class='username', href='/profile?u='+username)
        | #{post.tweet}
        i posted #{post.date} ago via web
    - })
